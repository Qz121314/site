# Optional for local/PR builds; production deploys require the matching GitHub Actions repository variable.
# Set this to the active HTTPS R2 Custom Domain / CDN root origin.
# Do not hardcode a production hostname in Storefront source.
VITE_PUBLIC_CONTENT_ORIGIN=
