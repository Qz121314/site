# Optional release/build-time origin for direct published Storefront JSON reads.
# Set this to the active R2 Custom Domain / CDN origin in the release environment.
# Do not hardcode a production hostname in Storefront source.
VITE_PUBLIC_CONTENT_ORIGIN=
