{
  "name": "@site/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../../wrangler.jsonc",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "for f in test/*.test.mjs; do node --test --experimental-strip-types --import ./test/register-ts-resolver.mjs \"$f\" || { echo \"::error file=apps/worker/$f,title=Worker test failed::$f\"; exit 1; }; done"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.20.0"
  }
}
