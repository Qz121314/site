name: Apply chat media gateway

on:
  push:
    branches: [feat/chat-media]
    paths:
      - scripts/patch-storefront-media-gateway.mjs
      - .github/workflows/apply-chat-media.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/chat-media
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --global pnpm@11.19.0
      - run: pnpm install --frozen-lockfile
      - run: node scripts/patch-storefront-media-gateway.mjs
      - run: pnpm exec prettier --write apps/storefront/src/support-gateway.ts
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/storefront/src/support-gateway.ts
          git commit -m "接入访客图片媒体网关"
          git push origin HEAD:feat/chat-media
