name: Format Inspect

on:
  pull_request:

permissions:
  contents: read

jobs:
  inspect:
    if: github.head_ref == 'fix/storefront-navigation-direction'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        run: npm install --global pnpm@11.19.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Print exact Prettier diff
        run: |
          pnpm exec prettier apps/storefront/src/StorefrontPresentation.tsx apps/storefront/test/navigation-direction-contract.test.mjs --write
          git diff -- apps/storefront/src/StorefrontPresentation.tsx apps/storefront/test/navigation-direction-contract.test.mjs || true
