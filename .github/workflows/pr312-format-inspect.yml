name: PR 312 Format Inspect

on:
  pull_request:

permissions:
  contents: read

jobs:
  inspect:
    if: github.head_ref == 'style/storefront-app-chrome-controls'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        run: npm install --global pnpm@11.19.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Print exact Prettier diff
        run: |
          pnpm exec prettier apps/storefront/src/app-chrome.css apps/storefront/test/type-buttons-motion-contract.test.mjs --write
          git diff -- apps/storefront/src/app-chrome.css apps/storefront/test/type-buttons-motion-contract.test.mjs || true
