name: Product CTA Format Preflight

on:
  push:
    branches: [style/product-detail-fixed-cta]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: style/product-detail-fixed-cta

      - name: Set up pnpm
        run: npm install --global pnpm@11.19.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply repository Prettier
        run: |
          set -euo pipefail
          pnpm exec prettier \
            apps/storefront/src/product-detail-ui.css \
            apps/storefront/test/product-detail-conversion-contract.test.mjs \
            apps/storefront/test/production-visual-system-contract.test.mjs \
            tests/e2e/production-smoke.spec.ts \
            --write
          if git diff --quiet -- \
            apps/storefront/src/product-detail-ui.css \
            apps/storefront/test/product-detail-conversion-contract.test.mjs \
            apps/storefront/test/production-visual-system-contract.test.mjs \
            tests/e2e/production-smoke.spec.ts; then
            echo 'Formatting already clean.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            apps/storefront/src/product-detail-ui.css \
            apps/storefront/test/product-detail-conversion-contract.test.mjs \
            apps/storefront/test/production-visual-system-contract.test.mjs \
            tests/e2e/production-smoke.spec.ts
          git commit -m 'chore: apply fixed CTA prettier formatting'
          git push origin HEAD:style/product-detail-fixed-cta
