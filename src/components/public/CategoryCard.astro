---
import type { PublicCategory } from "@/lib/db/public";

interface Props {
  category: PublicCategory;
  channelSlug: string;
}

const { category, channelSlug } = Astro.props;
const itemLabel = `${category.productCount} ${category.productCount === 1 ? "item" : "items"}`;
---

<a
  class="visual-card category-card"
  href={`/${encodeURIComponent(channelSlug)}/category/${encodeURIComponent(category.slug)}`}
  data-category-card
  data-filter-ids={category.filterIds.join(",")}
  aria-label={`${category.name}, ${itemLabel}`}
>
  {category.imageUrl ? (
    <img class="visual-card-media" src={category.imageUrl} alt="" loading="lazy" />
  ) : (
    <span class="visual-card-placeholder" aria-hidden="true">{category.name.trim().slice(0, 1).toUpperCase() || "C"}</span>
  )}
  <span class="visual-card-overlay">
    <span class="visual-card-title">{category.name}</span>
  </span>
  <span class="visual-card-arrow" aria-hidden="true">
    <svg viewBox="0 0 24 24">
      <path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" />
    </svg>
  </span>
</a>
