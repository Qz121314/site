---
export const prerender = false;
const channelId = Astro.params.channelId ?? "";
return Astro.redirect(
  channelId
    ? `/admin/channels/${encodeURIComponent(channelId)}/filters`
    : "/admin/channels",
  302,
);
---
