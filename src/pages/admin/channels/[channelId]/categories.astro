---
export const prerender = false;
return Astro.redirect(
  Astro.params.channelId
    ? `/admin/channels/${encodeURIComponent(Astro.params.channelId)}/filters`
    : "/admin/channels",
  302,
);
---
