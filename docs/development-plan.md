# 开发阶段与交付计划

目标：先完成中文运营后台，再完成 English 用户前端。

## 已完成基础

- [x] React Admin 与 React Storefront；
- [x] Cloudflare Worker + Hono；
- [x] 正式 D1 与 R2；
- [x] 单管理员登录；
- [x] R2 自定义域名设置与测试；
- [x] 分区新增、编辑、排序、启停、删除和恢复；
- [x] 部署保留 Dashboard 手动变量。

## 阶段 1：后台信息架构

固定菜单：

```text
站点设置
素材库管理
客服管理
FAQ 管理
分区管理
```

动态分区菜单：

```text
[分区名称]
├─ 产品录入
├─ 分类管理
└─ 转化池
```

已完成：

- [x] 删除仪表盘、热门推荐、发布管理和操作日志主菜单；
- [x] 站点设置加入 GA4、FB Pixel 和联盟检测；
- [x] 客服配置独立落库并提供管理页面；
- [x] 新增分区内分类表；
- [x] 分类数据和管理接口按分区隔离；
- [x] 素材库明确为纯 R2 图片管理模块；
- [x] 动态菜单改为产品录入、分类管理和转化池。

## 阶段 2：素材库管理

素材库禁止上传，本质上通过后台管理 R2 中的图片。

已完成：

- [x] 扫描整个 R2 Bucket；
- [x] 识别并显示全部图片对象；
- [x] 使用 R2 cursor 自动扫描到结束；
- [x] 非图片对象不进入素材库；
- [x] 对比 D1 Logo、分区图标、产品封面和产品图片引用；
- [x] 所有图片只分为使用中和未使用；
- [x] 保护软删除分区和产品仍引用的图片；
- [x] 使用中和未使用 Tab；
- [x] 搜索、选择和当前结果全选；
- [x] 未使用图片批量物理删除；
- [x] 删除前重新读取 R2 metadata 与 D1 引用；
- [x] 使用媒体更新时间防止并发误删；
- [x] 批量清理使用幂等键；
- [x] R2 删除失败时恢复 D1 状态；
- [x] 记录完成、失败和对账异常审计。

完成标准：

```text
打开素材库管理
→ 自动扫描整个 R2 Bucket
→ 显示全部图片
→ 分为使用中和未使用
→ 选择未使用图片
→ 再次校验业务引用
→ 从 R2 物理删除
```

## 阶段 3：FAQ 管理

FAQ 保持简单，每条内容只有标题和正文。

已完成：

- [x] FAQ 列表；
- [x] 新增和编辑标题、正文；
- [x] 正文同时支持普通文本和 Markdown；
- [x] 编辑与安全预览切换；
- [x] 标题和正文搜索；
- [x] 单项物理删除；
- [x] 所有写操作进入审计日志；
- [x] 不增加排序、启停、批量操作或回收站。

## 阶段 4：分类管理

分类必须属于当前分区。

已完成：

- [x] 当前分区分类列表；
- [x] 新增和编辑名称、排序与启用状态；
- [x] 搜索、上移和下移；
- [x] 选择、当前结果全选和批量删除；
- [x] 软删除与当前分区回收站恢复；
- [x] 删除前检查所有产品引用；
- [x] API 按 sectionId 隔离，禁止跨分区读写；
- [x] 批量删除和排序使用幂等键；
- [x] 所有写操作进入审计日志。

## 阶段 5：转化池

每个分区有独立转化池。产品绑定转化分组，分组内绑定一个或多个同类型入口。

已完成：

- [x] 转化分组按分区隔离；
- [x] 分组类型为在线客服或外部链接；
- [x] 每个分组配置 CTA 按钮文字；
- [x] 每个分组绑定一个或多个客服/链接入口；
- [x] 入口启停、排序、选择、全选和批量删除；
- [x] 分组启停、排序、选择、全选和批量删除；
- [x] 分组和入口均支持模块内回收站恢复；
- [x] 顺序轮询游标与测试轮换；
- [x] 停用和回收站入口不参与轮换；
- [x] 分组已有入口时禁止修改分组类型；
- [x] 分组存在入口或产品引用时禁止删除；
- [x] 产品新增 conversion_group_id 关系；
- [x] 批量操作使用幂等键，所有写操作进入审计日志。

轮换模型：

```text
产品 CTA
→ 产品绑定的转化分组
→ 读取分组内启用入口
→ 按 sort_order 顺序循环选择
→ 在线客服：打开客服入口
→ 外部链接：跳转目标链接
```

## 阶段 6：产品录入

产品必须属于当前分区。

已完成：

- [x] 标题、Markdown 正文、线上/线下服务和地址；
- [x] 选择本分区分类；
- [x] 选择本分区转化分组；
- [x] 线下产品只能选择在线客服分组；
- [x] 线上产品只能选择外部链接分组；
- [x] 分组至少有一个启用入口后产品才能发布；
- [x] 产品图片选择后只在浏览器本地处理；
- [x] 浏览器压缩为最长边 1400px、质量 0.82 的 WebP，失败时回退 JPEG；
- [x] 原图不上传 R2，点击保存产品后才上传压缩图；
- [x] 本地预览、旋转、排序、移除和显式封面选择；
- [x] GIF 和 SVG 不进入原图上传流程；
- [x] 服务端继续校验压缩图 MIME、真实文件头、大小和尺寸；
- [x] 上传成功但产品保存失败时复用已上传素材，避免重复上传；
- [x] 未指定封面时自动使用第一张产品图片；
- [x] 热门状态、热门排序和产品排序；
- [x] 草稿、发布和归档；
- [x] 搜索和发布状态筛选；
- [x] 选择、当前结果全选和批量删除；
- [x] 软删除与回收站恢复；
- [x] 所有 API 按 sectionId 隔离；
- [x] 批量删除和排序使用幂等键；
- [x] 所有写操作进入审计日志。

图片保存流程：

```text
选择本地 JPG / PNG / WebP
→ 浏览器压缩并生成本地预览
→ 本地旋转、排序、移除、设置封面
→ 点击保存产品
→ 只上传压缩后的 WebP / JPEG 到 R2
→ 保存产品与图片引用
```

发布条件：

```text
分类有效并启用
+ 转化分组类型匹配服务类型
+ 转化分组启用且至少有一个启用入口
+ 至少一张产品图片
+ 线下服务填写地址
```

## 阶段 7：Logo 与业务图片

已完成：

- [x] 产品图片采用浏览器本地压缩、保存时上传；
- [x] 素材库仍然不提供上传；
- [x] 产品图片 MIME、大小和尺寸校验；
- [x] 产品对象 Key 和公开 URL 统一；
- [x] 重复图片按内容哈希复用。

下一任务：

- [ ] 站点设置从 R2 对象选择 Logo；
- [ ] 分区图标从 R2 对象选择；

## 阶段 8：English Storefront

- [ ] 动态分区导航；
- [ ] 产品列表和详情；
- [ ] 分类筛选；
- [ ] 热门产品；
- [ ] FAQ；
- [ ] CTA 轮换解析与转化事件记录；
- [ ] GA4、FB Pixel 和联盟检测；
- [ ] PC 两列、移动端单列。

## 当前优先级

```text
P0  Logo 与分区图标选择
P1  English Storefront
```
