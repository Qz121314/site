# 开发阶段与交付计划

目标：先完成中文运营后台，再完成 English 用户前端。

## 已完成基础

- [x] React Admin 与 React Storefront；
- [x] Cloudflare Worker + Hono；
- [x] 正式 D1 与 R2；
- [x] 单管理员登录；
- [x] R2 自定义域名设置与测试；
- [x] 分区新增、编辑、排序、启停、删除和恢复；
- [x] 部署保留 Dashboard 手动变量。

## 阶段 1：后台信息架构

固定菜单：

```text
站点设置
素材库管理
客服管理
FAQ 管理
分区管理
```

动态分区菜单：

```text
[分区名称]
├─ 产品录入
├─ 分类管理
└─ 转化池
```

已完成：

- [x] 删除仪表盘、热门推荐、发布管理和操作日志主菜单；
- [x] 站点设置加入 GA4、FB Pixel 和联盟检测；
- [x] 客服配置独立落库并提供管理页面；
- [x] 新增分区内分类表；
- [x] 产品分类关系阻止跨分区引用；
- [x] 素材库明确为纯 R2 扫描清理模块；
- [x] 动态菜单改为产品录入、分类管理和转化池。

## 阶段 2：素材库管理

素材库禁止上传。

已完成：

- [x] 只扫描 R2 `media/` 对象；
- [x] 排除 `public/` 发布文件和其他前缀；
- [x] 使用 R2 cursor 分页继续扫描；
- [x] 对比 `media_assets` 和业务引用；
- [x] 标识使用中、未使用和未登记对象；
- [x] 保护软删除分区和产品仍引用的图片；
- [x] 搜索、筛选和继续扫描；
- [x] 选择、当前筛选结果全选和批量清理；
- [x] 新上传对象保留 24 小时保护期；
- [x] 删除前重新检查 R2 metadata 与 D1 引用；
- [x] 使用媒体更新时间防止并发误删；
- [x] 批量清理使用幂等键；
- [x] R2 删除失败时恢复 D1 状态；
- [x] 记录完成、失败和对账异常审计。

完成标准：

```text
打开素材库管理
→ 扫描 media/ 图片
→ 查看 D1 引用状态
→ 选择确定未使用且超过保护期的图片
→ 再次校验
→ 永久清理 R2 对象
```

## 阶段 3：FAQ 管理

下一任务：

- [ ] FAQ 列表；
- [ ] 新增和编辑；
- [ ] 排序和启停；
- [ ] 搜索和筛选；
- [ ] 选择、当前页全选和批量删除；
- [ ] 软删除与回收站恢复；
- [ ] 所有写操作进入审计日志。

## 阶段 4：分类管理

分类必须属于当前分区。

- [ ] 列表、新增和编辑；
- [ ] 排序和启停；
- [ ] 选择、全选和批量删除；
- [ ] 回收站恢复；
- [ ] 删除前检查产品引用。

## 阶段 5：转化池

转化池必须属于当前分区。

- [ ] URL、电话、邮箱和自定义类型；
- [ ] 新增、编辑、排序和启停；
- [ ] 选择、全选和批量删除；
- [ ] 回收站恢复；
- [ ] 禁止删除仍被产品引用的项目。

## 阶段 6：产品录入

产品必须属于当前分区。

- [ ] 标题、正文、线上/线下和地址；
- [ ] 产品图片与封面；
- [ ] 选择本分区分类；
- [ ] 选择本分区转化池；
- [ ] 热门状态和排序；
- [ ] 草稿、发布和归档；
- [ ] 选择、全选、批量删除和恢复。

## 阶段 7：Logo 与业务图片

- [ ] 站点设置从 R2 对象选择 Logo；
- [ ] 产品录入直接上传产品图片到 R2；
- [ ] 素材库仍然不提供上传；
- [ ] MIME、大小和尺寸校验；
- [ ] 统一对象 Key 和公开 URL。

## 阶段 8：English Storefront

- [ ] 动态分区导航；
- [ ] 产品列表和详情；
- [ ] 分类筛选；
- [ ] 热门产品；
- [ ] FAQ；
- [ ] 客服入口；
- [ ] GA4、FB Pixel 和联盟检测；
- [ ] PC 两列、移动端单列。

## 当前优先级

```text
P0  FAQ 管理
P1  分类管理
P2  转化池
P3  产品录入
P4  Logo 与业务图片
P5  English Storefront
```
